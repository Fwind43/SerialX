# SerialX v0.0.8 Plan

## Goal

Focus `v0.0.8` on feature delivery and stabilization on top of the unified theme and appearance workflow completed in `v0.0.7`.

## Principles

- Keep development on `codex/v0.0.8-dev`
- Treat theme and appearance as one unified system
- Record every confirmed bug fix in the version bug log
- Prefer incremental, reviewable changes

## Workstreams

### 1. Feature Development

- Implement scoped `v0.0.8` features requested during the cycle
- Keep UI and state changes aligned with the unified theme model

### 2. Bug Fixing

- Fix newly reported issues during the `v0.0.8` cycle
- Document each fixed bug in `docs/v0.0.8-bug-log.md`

### 3. Release Readiness

- Keep version notes current through development
- Verify builds before release tagging

## Tracking

- Branch: `codex/v0.0.8-dev`
- Bug log: `docs/v0.0.8-bug-log.md`
