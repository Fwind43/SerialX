# SerialX v0.0.5 任务清单

本清单用于配合 [v0.0.5-plan.md](/E:/SerialX/docs/v0.0.5-plan.md) 推进开发。

状态说明：

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成

## M1 稳定性收口

### 连接与状态同步

- [-] 梳理串口连接、断开、异常断开的统一状态流转
- [x] 优化端口刷新后的当前选中保留逻辑
- [x] 优化连接失败、端口占用、发送失败的界面提示
- [-] 检查侧栏选中、活动标签、当前面板三者同步逻辑

### 分屏与布局回收

- [-] 继续整理 `pane` 宽度状态模型
- [x] 修复关闭空 `pane` 后的布局残留问题
- [-] 校验分屏模式下断开连接后的自动回收逻辑
- [ ] 校验窗口缩放后分屏宽度归一化逻辑

### 验证项

- [ ] 验证多串口同时连接/断开场景
- [ ] 验证分屏后关闭、断开、切换的边界情况
- [ ] 验证端口刷新时的会话连续性

## M2 高频操作提效

### 终端搜索与复制

- [x] 支持 `Enter` 跳转下一项匹配
- [x] 支持 `Shift+Enter` 跳转上一项匹配
- [x] 优化搜索浮层键盘交互与焦点行为
- [x] 整理终端右键菜单项
- [ ] 复查搜索高亮与文字可读性

### 发送体验

- [x] 为发送输入补充快捷键行为
- [ ] 优化发送失败和发送成功的即时反馈
- [ ] 增加循环发送启动延时
- [ ] 增加循环发送失败阈值与自动停止
- [ ] 优化常用命令区调用路径

### 性能与响应

- [ ] 评估大量日志写入时的终端响应
- [ ] 评估大量日志场景下搜索响应表现

## M3 设置与会话整理

### 设置结构

- [ ] 梳理基础设置、终端设置、高级设置的分层方式
- [ ] 整理设置入口与弹窗内容层级
- [ ] 减少低频设置对主界面的打扰

### 数据与恢复

- [ ] 优化设置导入的错误校验
- [ ] 优化设置导出的版本兼容信息
- [ ] 设计工作区布局恢复的数据结构
- [ ] 补充关键会话状态的持久化恢复

### 后续预留

- [ ] 为日志导出预留数据接口
- [ ] 为会话快照功能预留状态结构

## 建议执行顺序

1. 先完成 `M1`
2. 再推进 `M2`
3. 最后收口 `M3`

## 开发记录

- [x] 建立 `v0.0.5` 计划文档
- [x] 建立 `v0.0.5` 任务清单文档
